# 限定canary执行状态：未通过

2026-09-09，用户批准计划 `61faa403c88032f3633e7b9a354101e8bdfe85a312c04dc8bca6d2f1a3f1f89d`：3份冻结虚构文档、最多14次文档嵌入+8次查询嵌入+8次重排，无重试、无回答模型、生产仅配置只读。

## 已验证

- 编排预检核验冻结plan、3文档/48片段/8题及远端脚本可解析；执行前创建独占阶段标记，存在标记禁止重跑。
- 首次假向量演练在Docker未运行时停于资源创建前；恢复已有OrbStack后，同一固定镜像/内部网络的假向量演练通过，生成8题候选，容器/网络清理成功。假向量与真实产物使用独立目录及明确mock标志，不允许互相替代。
- 本机候选阶段使用临时表、原生pgvector/tsvector和项目RRF；不是完整产品Hybrid执行器或Assistant回答验收。计划中的人工标签只用于映射期望身份，不冒充实际回答或深读。

## 真实批次失败

真实embed阶段仅启动一次。接收返回后，JSON.parse报 `Expected ']'`，未获得可解析终态或向量产物。原编排器在解析前未保存stdout，故无法恢复该回传或核验实际完成的调用数；不得声称22次已完成，也不得声称0次。最多22次嵌入授权保守视为已消耗/不确定，不自动补偿或重跑。rerank阶段未启动；真实向量的本机PG验收未执行。无任何通过率、Recall或precision结论。

一次性started及人工记录的uncertain终态留在被Git忽略的受限本地目录；终态明确confirmedCompletedCalls=null、retryAllowed=false。本轮代码没有生产写入或配置变更、Worker/索引任务创建、S3、来源或回答模型调用。

## 离线改进及剩余边界

后续代码改为await写出完整stdout，并在本机先保存受限received文件再解析，传输异常写入安全失败终态。仅完成脚本解析预检；本地1MiB合成管道对照中console与awaited方式都完整成功，未重现远端问题，不能宣称已确认或修复根因。未用修订版重跑真实模型。

后续纯合成SSH/Docker大报文和新校验帧验证结果见[transport-diagnostic.md](transport-diagnostic.md)：全部字节/hash一致，未重现原故障；新完整性保护已加入，但不把它称作原故障已定位。新批次必须重新审批并绑定新执行身份；不得删除本批次标记以重用授权。真实canary及完整MVP仍未通过，生产不部署。
