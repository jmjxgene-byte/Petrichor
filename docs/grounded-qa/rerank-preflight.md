# 重排阶段预检

2026-09-10：基于已验收嵌入批次 `8df5206f-b37a-4ac0-af2c-71b2dca47ad6` 的离线BM25/语义/RRF候选，生成重排请求预检；没有访问网络、数据库或模型。预检固定模型标识 `BAAI/bge-reranker-v2-m3`，但当前 `rerankerProfileVerified=false`，因此不代表生产重排配置已经可用。

共生成8个候选请求，覆盖固定8题；每个请求最多20个片段，最大请求约40,139字节，总输入约188,722字节，无空候选跳过。请求正文只在本机内存中用于计算稳定哈希，安全预检文件 `.data/embedding-approved/rerank-preflight-v2.json` 仅保存请求集哈希、候选数量、字节数、case键和模型调用/数据库调用计数，不保存查询、片段或向量。

预检使用与provider适配器相同的严格字段和限制（`return_documents=false`、`top_n`等），候选集合变化会导致request-set哈希变化。8项定向测试、全量1628通过/40既有跳过，typecheck/lint/build/audit/diff均通过，构建大chunk提示保留。

下一门需先由受控运行时读取并核验独立的重排模型/凭证配置，再单独授权真实重排；失败时保留安全终态，不重用旧失败模型批次，不写生产索引。即使重排通过，也只完成组件验收，仍需完整60题人工评测和真实索引/问答链路验收。

同时新增结果消费契约：只接受连续候选索引和非递增有限分数，映射为安全候选ID并计算gold命中率；不保存query、候选正文、向量或模型回显。7项重排消费测试及完整门禁通过，仍未发出真实重排请求。
