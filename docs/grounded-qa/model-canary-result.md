# 合成模型能力探针结果

2026-09-08，用户批准使用既有硅基流动凭证，在服务器进程内临时测试非Pro嵌入和重排；不保存新配置、不导出凭证、不写生产数据库。执行前通过只读事务核验绑定、模型维度和目标端点；API请求固定HTTPS目标、禁止重定向、每请求20秒超时、响应上限256KiB，无重试。

官方[价格页](https://siliconflow.cn/pricing)本轮查询结果将 `BAAI/bge-m3` 和 `BAAI/bge-reranker-v2-m3` 均标为免费，与收费的Pro版本区分。该记录是当次公开定价，不是永久价格承诺或实际账单证明。

## 实际执行

| 探针 | 请求数 | 验证结果 | 耗时 | 用量 |
| --- | ---: | --- | ---: | --- |
| BAAI/bge-m3 | 1 | 4个向量，全部1024维、有限且非零，返回index唯一且在输入范围内 | 1707ms | total_tokens=68 |
| BAAI/bge-reranker-v2-m3 | 1 | 4个候选index唯一且有效，分数有限且降序；top index=2，属于相关候选 | 352ms | 未记录，不声明为0 |

总模型请求数恰好2，全部成功。没有创建索引Job、启动Worker、修改绑定/env/数据库或传输真实资料。输入脚本通过stdin执行，未在远端落盘；本地临时脚本不含凭证，执行后删除。未保留向量或模型原始响应，只记录上述安全聚合。

## 固定虚构输入

embedding一次发送以下四段文本；rerank使用相同四段作为documents、查询“仓库收到退货后应如何处理？”，top_n=4、return_documents=false。

1. 蓝鲸仓库的退货包裹需先核对订单号，再登记入库。
2. 小熊咖啡店每周一清洁磨豆机。
3. 蓝鲸仓库收到退件后，应验证订单并记录退货入库。
4. 星河图书馆周末举办儿童阅读活动。

## 证明边界与下一步

这证明固定接口可接受小输入并返回契约兼容结果，不是SDK端到端验收、语义模型对比、真实文档效果、人工引用支持precision或P95测量。重排仅为本次进程内候选，生产重排配置仍未启用。

下一阶段先接通MVP的重排配置与安全凭证复用，再独立授权有限索引canary和固定60题评测；包括人工相关证据/引用审阅、失败降级、版本切换及Staging真实用户链路。没有通过这些门禁前不部署完整MVP、不全量向量化。
