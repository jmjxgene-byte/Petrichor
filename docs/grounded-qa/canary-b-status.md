# Canary 20260909-b：回传截断，未通过

用户单独授权新批次，仍绑定plan `61faa403c88032f3633e7b9a354101e8bdfe85a312c04dc8bca6d2f1a3f1f89d`，上限14次文档嵌入+8次查询嵌入+8次重排。新目录及executionId与原批次隔离，原started/terminal SHA核对未变。

## 可核验证据

- 预检unused=true；本机两种固定arm64镜像可用。
- embed仅启动一次。独立stderr进度记录完整包含ATTEMPT/RESPONSE 1至22，无重试；重排未启动。
- 总执行达到600秒时仍未返回完整产物，SSH退出255。
- 回传头声明JSON负载 **1680201字节**，负载SHA `9d489e6c0b547ef90888a84f8be805157f09def6be49e5452dc2ba23c62bb885`。
- 实际stdout **65537字节**，SHA `fb3a2de4aecbb000b3d72713b09014b800986c872ebfdf6893688e7661e0b097`，缺END标记；原帧及进度元数据已在受限本地目录保留。
- 22次请求有响应记录不等于本机已获得可用向量集合；没有合法embed.json，未进入真实PG候选生成或rerank，不能计算Recall/precision。

## 清理与状态

宿主超时后发现本批次临时`bun run -`仍存活；连续核验精确PID/命令后仅TERM该进程。复查容器只剩原Web主进程及init，Web为healthy，未重启或替换Web；没有创建Worker或生产索引任务。旧批次标记未删除/覆盖。

新批次保留started、transport.json、received.frame和不可重试失败终态，不再次执行22次嵌入，也不把未使用的8次rerank权限转作别的调用。生产仅配置只读，未修改数据库或运行配置。

## 判断与下一步

本轮已将故障缩小到模型响应之后的大产物回传：卡在约64KiB，仍不能仅凭此确定Bun、Docker或SSH中的具体根因。此前约1.4MB纯合成传输成功不能覆盖这次真实异步执行形态。

停止真实批处理，下一步使用与1680201字节及异步执行形态匹配的纯合成夹具诊断，并考虑带持久化收据的分段传输；不能用真实模型反复测试运输层。任何新的真实批次须另批，完整MVP不部署。
