# Canary回传诊断

2026-09-09，仅运行纯合成大报文，不访问环境变量内容、数据库、模型、业务资料，不重跑失败canary，不修改线上容器配置或重启服务。脚本通过stdin运行，远端不落文件；本地只保存字节数、SHA、退出码等元数据。

## 结果

| 路径 | 发送/接收字节 | SHA一致 | JSON/退出码 |
| --- | ---: | --- | --- |
| SSH→Docker→Bun console输出 | 1416738 / 1416738 | 是 | 有效 / 0 |
| 同路径await写完 | 1416738 / 1416738 | 是 | 有效 / 0 |
| 大stdin脚本+大stdout | 1416738 / 1416738 | 是 | 有效 / 0 |
| 新校验帧 | 1416900 / 1416900 | 是 | 有效 / 0 |

前三种原文SHA均为 `2c9d9b29058938333edec4b52e459dd62a65f293935204cc2558f1bd20965df4`；新帧SHA为 `772fb47273acded8054c6c31d863277cae3584d8ee0edbc50fc108377bfc4676`。stderr字节数均0。没有重现原故障，不能断言原故障由console.log或SSH截断造成。

首轮诊断把6字中文长度误写为7，导致jsonValid误报false，但字节/hash始终相同；修正为实际字符串length后完整复验通过。最初新测试放在scripts目录，Vitest未发现，已移动到项目src测试目录；未改测试发现规则或弱化断言。

## 新保护

`canary-transport.ts` 的协议包含版本、UTF-8字节数、SHA256及结束标记，最多4MiB；截断、内容损坏、缺尾或附加输出全部拒绝。完整帧不代表业务成功，后续仍核验JSON、planHash、phase、调用上限和退出码。

provider编排先保存退出码、stdout/stderr字节及SHA，再保存受限原始帧、解析结果；等待stdin结束和stdout写出，保留原有独占started标记及不可重试失败终态。诊断脚本没有数据库或模型导入。上述新帧只用合成报文验证，未重跑真实模型。

原批次仍是uncertain、实际完成调用数null、rerank未启动；因为原stdout在解析前未保存，无法追溯恢复该产物。下一批次必须有独立授权和执行身份，不得删除原标记来绕过一次性边界。完整MVP尚未通过真实canary或发布门。
