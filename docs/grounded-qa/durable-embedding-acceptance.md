# 独立持久化嵌入批次验收

2026-09-09，用户明确授权新的22次合成样本嵌入请求后，执行独立批次 `8df5206f-b37a-4ac0-af2c-71b2dca47ad6`。没有重用或删除两个旧失败批次标记。使用已核验的管理员嵌入配置，固定BGE-M3/1024与原合成请求集；新增具名执行身份支持及单项宿主入口。API Key仅在现有Web运行时内部解密使用，没有传给宿主、本机、日志或模型输入。

## 本次结果

- 14次文档嵌入、8次查询嵌入，共22次请求，全部成功，无重试。
- 每项先在运行时保存，再由宿主分块校验并ACK，最后由本机接收并独立校验后才推进下一项。
- 48个文档向量、8个查询向量，均为1024维、有限非零float32值；全部收据、manifest与产物哈希一致。
- 合计产物1,218,835字节，单项最大87,207字节；本次分块路径完整通过，不代表已确定旧stdout截断根因。
- 服务响应记录合计22,885 tokens，22项均有用量；账单金额未核实，costUsd保持null。
- 重排调用0、生产索引写入0，未部署、未重启Web、未启动Worker。
- 22项宿主状态全部persisted且ACK=true。独立审计后精确删除本次运行时临时目录，复核Web健康、镜像和启动实例未变；宿主root-only和本机私有结果档案保留供离线检索评测。

固定运行时入口SHA：`c1506a91ead5f798cb9f2acd6ecf38a65182005dd1f960bb52cff16217683920`；宿主入口SHA：`8db9589326810fc024bda0c1226b3050fc8297599ce1c4d47340a0f70bd3056f`。配置指纹、连接信息及原始远端元数据不进入本报告。批次已结束，不得重建该身份重新调用。

本地验证：37项定向、全量1602通过/40既有跳过，typecheck/lint/build/diff通过；既有大chunk构建提示保留。此验收证明真实模型调用、响应完整性及持久化交接，不证明Recall、引用支持度或用户问答质量。固定小样本仅3篇合成文档、8题，不能替代完整60题人工评测。

下一步只使用已保存向量做离线召回评测，检查锚点映射与新批次消费契约；重排、真实索引库验收、Staging和生产发布仍分别保留审批门。

## 离线消费与锚点评测

2026-09-10新增`canary-embedding-consumer.ts`，严格读取本批次terminal、manifest、receipt和artifact，重新验证整批请求顺序、响应字段、向量维度、float32值、非零约束和所有SHA，再映射为48个文档passage及8个查询向量。它只在内存中运行BM25、余弦语义排序和RRF融合，围绕命中passage调用现有`buildEvidenceWindow`，不访问数据库、网络或模型。

在固定3篇合成文档/8题上，6题含工程草稿证据且全部映射成功（unmapped=0）；6题的词法、语义和RRF Top-20均命中目标。`late-1`命中`synthetic-topic-1:45`，窗口覆盖39–45片段，anchor范围在4,000字符预算内。2题分别要求澄清和不足，未产生证据召回。结果报告为`.data/embedding-approved/consumer-report-v2.json`（目录0700、文件0600），只保存安全元数据和候选ID；旧同名报告未覆盖。

该结果是离线组件契约和锚点定位通过，不是60题人工评测、Postgres真实索引、重排或问答正确率验收。样本过小且gold来自工程草稿，不能宣称Recall@20达到MVP门槛，也不能证明语义优于词法。下一步冻结消费格式并准备重排请求清单；真实重排仍需单独授权，不能使用旧失败批次或修改生产索引。
