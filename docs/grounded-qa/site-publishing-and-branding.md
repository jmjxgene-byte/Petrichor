# 全站星图发布与二开站点信息

## 已确认的星图问题与修复

2026-09-10截图为空图、0节点/关系且未运行抽取Agent，校验报missing_root。代码确认全站星图输入是公开分享且符合公开读取条件的文章，不是本地文档或外部GeneOps数据；generateSiteGraph无可用公开文章时直接拒绝，正常抽取链会建立root。publishSiteGraph仍强制重新校验，不能靠评分或前端操作绕过。

本次修复的是引导与状态表达：空图评分由80改为0，明确提示“先公开分享文章→Agent生成→校验→发布”；空图、未校验或校验失败时禁用发布按钮，显示可访问的原因说明。没有自动公开任何资料、补造节点或运行Agent。源码测试和构建通过，但尚未部署或完成浏览器验收，线上数据没有改变。

## 改造前的作者信息定位

| 显示内容 | 改造前入口/来源 |
| --- | --- |
| 名称、副标题、自我介绍、Expertise、Toolkit、引用语、联系方式 | 系统管理→关于我，`/dashboard/admin/about` |
| 关于页头像 | `AboutPage.tsx`引用静态`/about-avatar.png`，目前无后台编辑入口 |
| 页脚RSS/Email、版权年份/名称、Powered by个人署名、GitHub图标 | `RetypesetSiteChrome.tsx`写死 |
| 图3一键部署/源码/License、演示站点及项目宣传文案 | `/petrichor`静态项目介绍页，`PetrichorPage.tsx`常量，不是本站发布/部署控制台 |
| 加载失败时出现原作者简介、部分SEO作者文案 | AboutPage/服务端about默认值及public-site/metadata.ts |

## 品牌设置实现（功能分支完成，尚未上线）

已在“外观设置”（`/dashboard/admin/appearance`）增加“站点品牌”区：公开页标题/副标题、版权名称/起始年、站点维护者、联系文字/地址、源码仓库、头像地址，以及联系/维护署名/项目宣传页/源码按钮四个开关。默认隐藏未配置的联系方式和署名，项目页关闭时直接访问也只显示停用提示；原Vercel推广按钮移除。署名使用“站点维护”，不冒充原软件作者；页脚保留Petrichor/Apache-2.0来源声明。公开页组件共享配置读取并合并请求，后台保存更新当前页面缓存。

“关于我”编辑页与既有内容保留，新增头像地址位于统一品牌设置；介绍、技能和引用语支持清空。新安装默认值和前后台失败回退均改为中性内容，但不覆盖旧库已有资料：如已有原作者简介，请管理员在“关于我”手动编辑。联系方式只允许简单mailto或HTTPS，仓库只允许HTTPS，头像允许HTTPS/站内路径；拒绝危险协议、凭证URL与编码控制字符。已移除相关SEO描述中的个人名字，但文章SEO标题生成方式未重构。LICENSE、NOTICE及历史已执行迁移保持不变。

新增`branding_json`文本列及`2026-09-10-site-branding.sql`，已登记manifest，同时同步新安装Schema/初始化SQL；没有对旧资料执行UPDATE。旧客户端未提交branding时保留已有品牌，品牌更新必须显式携带publicQaEnabled，避免开启AI问答。未迁移旧库读取时只对42703/branding_json缺列做兼容，并保留真实问答开关；权限错误不吞掉。保存仍需先应用迁移。

准备阶段Supabase CLI因用户级遥测写入被权限边界拦住，因此按项目原有docs迁移清单准备SQL。随后已用固定镜像在本机合成PG17完成新增迁移/RLS回归，未连接或修改生产数据库。

关于页蓝色便签的联系方式也已改为读取品牌开关和链接，与页脚一致。旧联系方式字段只保留管理端兼容存档；公开about接口不再返回旧联系字段内容，公开appearance在开关关闭时清空联系和维护者输出，不影响管理端保存值。

本地验证：全量1622通过/40既有跳过，typecheck/lint/build/diff通过，既有大chunk警告保留。浏览器使用实际构建前端与仅本机模拟接口，验证保存/刷新、前台署名与联系显示、键盘清空隐藏、非法链接拒绝、项目页停用和390px布局；未发现页面控制台错误。关于页便签统一开关及公开字段过滤另有组件/单元回归。浏览器标签与模拟服务已关闭。此浏览器测试不代表生产数据写入或真实数据库迁移通过。

后续状态：初次审计报告的12项高危/严重问题已通过固定安全依赖版本修复，冻结安装及高危审计通过；品牌迁移已完成本机隔离PG17升级/回滚/RLS验收。详见[安全与迁移验收](security-and-branding-migration.md)。生产迁移、Staging及完整检索质量门仍未完成，尚未部署或修改线上品牌数据。

## 检索主线同步进展

复用已验收的56个向量，完成3篇合成资料/8题的本机内存组件预评测：6题有参考证据，词法BM25、余弦语义和RRF融合Top20均找全工程草稿证据；参考内容在原文offset范围内可定位，映射缺失0。其余2题为澄清/无答案，未测试生成行为。没有模型/数据库调用，没有生产索引写入。样本不足且未经人工独立标注，词法已经满召回，不能声称语义显著提升，也不能当成完整60题或实际Postgres/问答链路验收。
