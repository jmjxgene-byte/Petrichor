当前目标是让 Petrichor 安全消费 GeneOps shared retrieval v2，同时保持 v1 Exact/Fuzzy 回滚路径；分支 `codex/geneops-timeline-v2` 的代码基线为 `2fcfb8f14` 并已推送，已实现按质量快照选择 search/read v1 或 v2、reply/anchor 深读、generation/snapshot/发布时间/timeline confidence 证据传播，以及时间未知时禁止模型断言“最新/先后”，持久化仍只保存安全引用元数据。UI、Hybrid/Wiki/Graph/Worker 均未改变或启用；既有验证为 Vitest 1,146 通过/40 跳过、typecheck、ESLint、production build 和 diff 检查通过，`@platejs/docx-io <53.3.2` 仍是独立发布前阻塞。GeneOps control-v2 在首波 2 条后因普通来源 `access_wall=1` 与付费来源 `source_unavailable=1` 停止，observation=0、后续 8 条未运行。GeneOps `2a7549303` 已实现固定两样本 diagnostic，`5bbb051bb` 已固定跨平台 tar identity，本地 74/74 + 34/34 与 CI `33901235604` 通过；完整 harness 传输、唯一无网络 smoke 和 preflight 均 PASS。全新任务 `01a06db1…12d57` 已执行唯一真实 diagnostic：PostgreSQL `READ ONLY`/`REPEATABLE READ` PASS，普通 r51_200 执行 1 次顶层 GET 后命中 `session_invalid`、延迟 `3_5s`，付费 r11_50 按即时停止规则未请求，总请求 1/2、status `stopped`；临时容器/runtime/package/env 清理完成、残留 0、Crawl 不变，未写数据库或保存响应。该证据仅证明页面出现登录墙标记，由于响应已丢弃，尚不能区分 Session 真过期与登录墙/登录态标记冲突。Petrichor 仍未部署且 v1 回滚与 Hybrid/Wiki/Graph/Worker 保持关闭；下一步应另行授权单请求 Session 探针，在此前不重跑两样本或恢复 canary。
