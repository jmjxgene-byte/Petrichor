当前目标是让 Petrichor 安全消费 GeneOps shared retrieval v2，同时保持 v1 Exact/Fuzzy 回滚路径；分支 `codex/geneops-timeline-v2` 的代码基线为 `2fcfb8f14` 并已推送，已实现按质量快照选择 search/read v1 或 v2、reply/anchor 深读、generation/snapshot/发布时间/timeline confidence 证据传播，以及时间未知时禁止模型断言“最新/先后”，持久化仍只保存安全引用元数据。UI、Hybrid/Wiki/Graph/Worker 均未改变或启用；既有验证为 Vitest 1,146 通过/40 跳过、typecheck、ESLint、production build 和 diff 检查通过，`@platejs/docx-io <53.3.2` 仍是独立发布前阻塞。GeneOps control-v2 在首波 2 条后因普通来源 `access_wall=1` 与付费来源 `source_unavailable=1` 停止，observation=0、后续 8 条未运行。GeneOps `2a7549303`/`5bbb051bb` 已实现两样本 diagnostic，远程 smoke/preflight PASS；唯一真实运行对普通 r51_200 发出 1 次 GET 后命中 `session_invalid`，付费样本未请求，残留 0、Crawl 不变、未写库或保存响应。GeneOps 提交 `5a006e2aa` 已新增独立 Session marker diagnostic：默认零请求 plan，真实执行只对 `probe_url` 发出 1 次 GET，分开输出 HTTP 类别、login-wall/logged-in-marker/access-wall、请求数与延迟桶，不保存 HTML/URL/凭据；本地与 CI `33908223469` 均通过 API 88/88、control/source/session runtime 45/45 和 Ruff，本阶段未连接数据库或来源。Petrichor 仍未部署且 v1 回滚与 Hybrid/Wiki/Graph/Worker 保持关闭；下一步需固定 Session marker package/harness 并独立完成远程 smoke/preflight，真实单请求探针仍需另行授权。
