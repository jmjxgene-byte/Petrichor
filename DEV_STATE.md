# Petrichor 可信问答 MVP 开发台账

2026-09-08目标active，完整实施范围见docs/grounded-qa/requirements.md与technical-design.md，用户已确认贴近真实UI的v2原型。独立分支codex/grounded-qa-mvp，回滚基线b6eac4c72/tag baseline/pre-grounded-qa-20260908已推送核验；代码节点5637b1af8（锚点深读）与1a11d6c70（资料必检索、无证据不生成）已推送。第三节点新增search-query.ts，复用中文词元/疑问词归一化，“翻新怎么翻？”召回翻新，摘要围绕命中；library-logic.ts统一参数化LIKE词项打分，两种数据库均排序后LIMIT，补充document和chunk双user过滤，删除SQLite无序预截断。新增read-budget.ts，在文档搜索路径复用既有池，以只读事务设置LOCAL statement_timeout/lock_timeout，继承Runtime到adapter的queryDeadlineAt，排队后检查截止/取消、返回前丢弃取消结果，不使用可能竞态的Postgres.js协议cancel；当前仅搜索接入，深读/外部/知识库的全链路超时仍未完成。新增11项词项/摘要/SQL构造/事务mock回归，最终1195通过/40既有跳过、typecheck/lint/build/diff检查通过；修正了本次替换时残留函数头和mock参数类型错误，未绕过检查。冻结安装与上一节点high审计通过，无锁文件变化，既有构建外部化/CommonJS/大chunk警告保留。未连接生产DB/来源/S3或业务模型，未启动Worker/迁移/部署；没有真实PG EXPLAIN或sleep超时fixture证据，LOWER(text)召回索引与P95还需离线实际库验证，不能用mock宣称性能门通过。仍待证据充分性/一次模型改写、generation/hash、锚点DB验证及UI恢复、版本化索引/Hybrid/Deep、60题人工评测与生产验收；截图Run仍只有UI记录，不声称零工具调用已证实。下一节点先补深读事务预算/数据库离线契约并开始版本化索引骨架与迁移，再推进Hybrid/接口/UI/Deep；生产迁移、真实模型批处理与Worker/发布继续独立审批。不修改其他工作树、GeneOps时间线/FNS或原文件/旧文档ID。
