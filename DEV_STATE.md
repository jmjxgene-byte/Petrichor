当前目标是让 Petrichor 安全消费 GeneOps shared retrieval v2，同时保持 v1 Exact/Fuzzy 回滚路径；分支 `codex/geneops-timeline-v2` 的代码基线为 `2fcfb8f14` 并已推送，已实现按质量快照选择 search/read v1 或 v2、reply/anchor 深读、generation/snapshot/发布时间/timeline confidence 证据传播，以及时间未知时禁止模型断言“最新/先后”，持久化仍只保存安全引用元数据。UI、Hybrid/Wiki/Graph/Worker 均未改变或启用；既有验证为 Vitest 1,146 通过/40 跳过、typecheck、ESLint、production build 和 diff 检查通过，`@platejs/docx-io <53.3.2` 仍是独立发布前阻塞。GeneOps `2a7549303`/`5bbb051bb` 已实现两样本 source-access diagnostic，远程 smoke/preflight PASS；唯一真实运行对普通 r51_200 发出 1 次 GET 后命中 `session_invalid`，付费样本未请求，残留 0、Crawl 不变、未写库或保存响应。GeneOps `5a006e2aa`/`e66b14538` 已实现 Session marker diagnostic 及固定 package/host harness，本地与 CI `33910374816` 均通过 88/88 + 53/53 和 Ruff。任务 `01a06de3…a0bed` 已验证公共 harness `c0bcd768…`、host harness `1abb5b06…`、package tar `a81c82f0…`，唯一无网络 smoke 和唯一 preflight 均 PASS，concurrency 1/request budget 1/retry 0，容器/runtime 各创建/清理 1、残留 0，Crawl 不变，未执行 Session marker、读环境值、连库或访问来源。Petrichor 仍未部署且 v1 回滚与 Hybrid/Wiki/Graph/Worker 保持关闭；下一步需单独授权真实 Session marker 单请求探针，在此前不重跑两样本或恢复 canary。
