当前目标是让 Petrichor 安全消费 GeneOps shared retrieval v2，同时保持现有 v1 Exact/Fuzzy 为回滚路径。生产自托管基线为 `f1c999661414f27111dcb4741e185f6ec22856db`，分支 `codex/geneops-timeline-v2` 从该 SHA 建立并已推送；已实现按外部源质量快照自动选择 search/read v1 或 v2、候选携带 reply/anchor、Evidence 传播 generation/snapshot/真实发布时间/timeline confidence，并在时间未知时明确禁止模型判断“最新/先后”，持久化仍只保存安全引用元数据，不保存 GeneOps 正文。Review 确认 UI 未改变、Hybrid/Wiki/Graph/Worker 未启用；使用校验过的 Bun 1.3.14 与 Codex 固定 Node 运行 Vitest，161 个文件通过、9 个跳过，1,146 项通过、40 项跳过，typecheck、ESLint、production build 和 `git diff --check` 均通过。安全审计仍有 `@platejs/docx-io <53.3.2` 一项需 Plate 52→53 跨主版本处理的独立发布前阻塞项。GeneOps 分支 `a90b2bf0a` 已为 5 个在用 legacy reader 增加 generation hardening；固定备份 schema-only 验收确认 6 条 migration 成功，Timeline/Retrieval 通过，generation filter 从 2/7 提升到 7/7。最后停止点是 runtime 测试自身用带参数名的 catalog 输出匹配仅类型字符串而误报，契约已改用完整 `regprocedure` 身份但遵循三次停止规则尚未重新执行，因此 5/5 legacy runtime probe 与 helper ACL/RLS 仍是部署阻塞；未通过该复验前不得将 v2 切为 current或部署 Petrichor v2 路径。尚未部署、未改生产 Supabase、未启动 Worker或模型。
