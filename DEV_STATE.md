当前目标是让 Petrichor 安全消费 GeneOps shared retrieval v2，同时保持 v1 Exact/Fuzzy 回滚路径；分支 `codex/geneops-timeline-v2` 的代码基线为 `2fcfb8f14` 并已推送，已实现按质量快照选择 search/read v1 或 v2、reply/anchor 深读、generation/snapshot/发布时间/timeline confidence 证据传播，以及时间未知时禁止模型断言“最新/先后”，持久化仍只保存安全引用元数据。UI、Hybrid/Wiki/Graph/Worker 均未改变或启用；既有验证为 Vitest 1,146 通过/40 跳过、typecheck、ESLint、production build 和 diff 检查通过，`@platejs/docx-io <53.3.2` 仍是独立发布前阻塞。GeneOps control-v2 在首波 2 条后因普通来源 `access_wall=1` 与付费来源 `source_unavailable=1` 停止，observation=0、后续 8 条未运行。GeneOps `2a7549303` 已实现固定两样本 diagnostic，`5bbb051bb` 已固定跨平台 tar identity，harness SHA `c0bcd768…b4cf431`，本地 74/74 + 34/34 和 CI `33901235604` 通过。Gene-OVH 的三个新任务均在运行前安全停止：分别是旧 gzip 身份门、无新 harness、无旧 harness 补丁基底；smoke/preflight 累计执行 0 次，容器/runtime 残留 0，Crawl 不变，未连接数据库或来源。本机已准备完整 harness 传输包：36,876 bytes 源文件 SHA `c0bcd768…b4cf431`，8,588 bytes gzip SHA `c783faf6…23082ac`，11,453 bytes base64 SHA `ae432f4d‧e3658c`，已通过解码回环与敏感信息扫描，尚未传输。Petrichor 仍未部署且 v1 回滚与 Hybrid/Wiki/Graph/Worker 保持关闭；下一步需单独授权完整 harness 传输后的新 smoke/preflight 任务，真实来源复验仍需另行授权。
