当前目标是让 Petrichor 安全消费 GeneOps shared retrieval v2，同时保持 v1 Exact/Fuzzy 回滚路径；分支 `codex/geneops-timeline-v2` 的代码基线为 `2fcfb8f14` 并已推送，已实现按质量快照选择 search/read v1 或 v2、reply/anchor 深读、generation/snapshot/发布时间/timeline confidence 证据传播，以及时间未知时禁止模型断言“最新/先后”，持久化仍只保存安全引用元数据。UI、Hybrid/Wiki/Graph/Worker 均未改变或启用；既有验证为 Vitest 1,146 通过/40 跳过、typecheck、ESLint、production build 和 diff 检查通过，`@platejs/docx-io <53.3.2` 仍是独立发布前阻塞。GeneOps control-v2 已通过 14 文件 package、CI、无网络 import smoke 和 preflight，但唯一 execute 在首个并发 wave 2 条后因普通来源 `access_wall=1` 与付费来源 `source_unavailable=1` 停止，observation=0、未运行后续 8 条；失败 spool 安全保留，容器/runtime/package 已清理，生产未写入、generation 未创建或切换。异常路径请求数被错误记录为 0 且失败 baseline 需要外部复核，故 Petrichor 本分支继续保持未部署、v1 回滚和所有 Hybrid/Wiki/Graph/Worker 关闭；下一步等待 GeneOps 修复审计字段并另行授权来源可用性复验，再决定检索评测和 v2 consumer 发布。
