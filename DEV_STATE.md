当前目标是让 Petrichor 安全消费 GeneOps shared retrieval v2，同时保持现有 v1 Exact/Fuzzy 为回滚路径。生产自托管基线为 `f1c999661414f27111dcb4741e185f6ec22856db`，分支 `codex/geneops-timeline-v2` 从该 SHA 建立；已实现按外部源质量快照自动选择 search/read v1 或 v2、候选携带 reply/anchor、Evidence 传播 generation/snapshot/真实发布时间/timeline confidence，并在时间未知时明确禁止模型判断“最新/先后”，持久化仍只保存安全引用元数据，不保存 GeneOps 正文。Review 确认 UI 未改变、Hybrid/Wiki/Graph/Worker 未启用；本轮使用校验过的 Bun 1.3.14 与 Codex 固定 Node 运行 Vitest，161 个文件通过、9 个跳过，1,146 项通过、40 项跳过；typecheck、ESLint、production build 和 `git diff --check` 通过，仅保留既有大 chunk/CommonJS 警告。安全审计已把 `fast-uri` 从 3.1.5 精确更新到 3.1.6并清除 4 项高危；仍有 `@platejs/docx-io <53.3.2` 一项高危，公告风险限于服务端/特权环境转换含远程图片的不可信 HTML，而当前调用在浏览器组件中，但修复要求 Plate 52→53 跨主版本，因此记录为独立发布前阻塞项，不在本次时间线分支冒险混升。尚未部署、未改生产 Supabase、未启动 Worker/模型；下一步提交并推送本分支，等待 GeneOps PostgreSQL 17 离线 migration/RLS 契约验收。
