当前目标是让 Petrichor 安全消费 GeneOps shared retrieval v2，同时保持 v1 Exact/Fuzzy 回滚路径；分支 `codex/geneops-timeline-v2` 的代码基线为 `2fcfb8f14` 并已推送，已实现按质量快照选择 search/read v1 或 v2、reply/anchor 深读、generation/snapshot/发布时间/timeline confidence 证据传播，以及时间未知时禁止模型断言“最新/先后”，持久化仍只保存安全引用元数据。UI、Hybrid/Wiki/Graph/Worker 均未改变或启用；既有验证为 Vitest 1,146 通过/40 跳过、typecheck、ESLint、production build 和 diff 检查通过，`@platejs/docx-io <53.3.2` 仍是独立发布前阻塞。GeneOps control-v2 在首波 2 条后因普通来源 `access_wall=1` 与付费来源 `source_unavailable=1` 停止，observation=0、后续 8 条未运行；上游 control-v3 smoke/preflight 台账已以 `4c2ba1080` 推送。GeneOps 上游提交 `2a7549303` 已离线实现只绑定原两条失败样本的 source-access diagnostic，默认 plan、并发 1、总预算 2、无重试，不输出 ID、URL、标题、正文或响应；GitHub Actions `33894579881` 在 CPython 3.13.15 下通过 API 74/74、control/runtime 24/24 及 Ruff，本阶段未连接数据库或来源。Petrichor 继续保持未部署、v1 回滚及 Hybrid/Wiki/Graph/Worker 关闭；下一步先等待 GeneOps 诊断最小 package/harness 的固定镜像无网络 smoke/preflight，再单独授权真实两条来源复验，过门前不恢复 canary、检索评测或 v2 consumer 发布。
