# Web / UI
NEXT_PUBLIC_REGISTER_ENABLED="false"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Go API（必填）：Next 将 /api、Feed、healthz 代理到该地址
PETRICHOR_GO_API_URL="http://127.0.0.1:8080"

# 数据库、认证、加密、S3、Redis 与 OAuth 等服务端配置请写入 apps/api/.env.local；
# 完整模板见 apps/api/.env.example。
